<p id="notice"><%= notice %></p>

<p>
  <strong>Url:</strong>
  <%= @url.url %>
</p>

<p>
  <strong>Doi:</strong>
  <%= @url.doi_id %>
</p>

<p>
  <strong>Date:</strong>
  <%= @url.date %>
</p>

<%= link_to 'Edit', edit_url_path(@url) %> |
<%= link_to 'Back', urls_path %>
